getPosition() 
获取玩家当前的 x 坐标。
x 坐标从西边的 0 开始，向东递增。

返回一个表示玩家当前 x 坐标的数字。

需要 1 tick 执行。

示例：
const pos = getPosition()
const x = pos.x
const y = pos.y 

更多相关信息请见：<u><link="doc/解锁/senses.md">Senses</link></u>
