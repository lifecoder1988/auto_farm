unlock(unlock) 
效果与在科技树中点击对应 unlock 的按钮完全相同。

如果解锁成功，则返回 True，否则返回 False。

如果成功，需要 200 ticks 执行，否则需要 1 tick。

示例：
unlock(Unlocks.Carrots)

更多相关信息请见：<u><link="docs/unlocks/auto_unlock.md">Auto_Unlock</link></u>