measure(direction = None) 
可以测量某些实体的一些值。效果取决于实体。

如果 direction 不为 None，则测量给定方向上的相邻实体。

返回向日葵的花瓣数量。
返回宝藏或苹果的下一个位置。
返回仙人掌的大小。
返回南瓜的一个神秘数字。
对所有其他实体返回 None。

需要 1 tick 执行。

示例：
num_petals = measure()

更多相关信息请见：<u><link="docs/unlocks/sunflowers.md">Sunflowers</link></u>