注释


被注释的内容在程序运行期间不会被执行。

在 JavaScript 中，可以使用 **//** 来添加单行注释。// 之后的所有内容都是注释，不会被程序执行。

// 这是一条注释

注释可以帮助你在代码中添加笔记，或者在不删除代码的情况下暂时禁用部分代码，用来调试程序。

在函数定义上一行的注释，也可以作为简单的文档说明（虽然不像 Python 那样能 hover 显示，但依然是良好习惯）。

// 这个函数什么也不做
function f() {
	// 空函数
}
