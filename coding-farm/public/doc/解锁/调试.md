调试

有时候代码无法正常运行，而你需要找出原因。以下工具会非常有帮助。


你可以使用 **console.log()** 来打印调试信息。  
它会将传入的所有值打印到输出窗口。

示例：

打印 `"0.24"`：
console.log(0.24)

打印 `"true"` 或 `"false"`：
console.log(canHarvest())

打印当前位置：
console.log(getPosition())

console.log 会将内容打印到输出窗口。

