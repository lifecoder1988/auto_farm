can_move(direction)
检查无人机是否可以向指定 direction 移动。

如果无人机可以移动，则返回 True，否则返回 False。

需要 1 tick 执行。

示例：
if can_move(North):
    move(North)

更多相关信息请见：<u><link="docs/unlocks/mazes.md">Mazes</link></u>